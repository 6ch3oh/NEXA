# NEXA Wave 006：用户后续操作清单

状态：`CODE_COMPLETE / HUMAN_ACCEPTANCE_REMAINS`

以下事项需要真实模型、真机、实体听音或用户选择，Wave 006 没有伪造通过，也没有因此阻塞其余施工。

## 1. 提供日历本地模型配置

- 问题：日历本地 AI Adapter、预览、差异、逐项确认、撤销和错误状态已经完成，但真实模型尚未配置。
- 用户需要做什么：提供 Provider、Base URL、Protocol、Model ID、Authorization mode、Credential reference、Health endpoint、Chat/Generate endpoint、是否 Streaming、是否支持 Structured JSON、Timeout 和最大并发数。凭据本身不要贴入配置文件或聊天，只提供凭据引用方式。
- 不做会影响什么：自然语言日历建议仍保持“本地日历助手尚未配置”；普通月历和手工日程不受影响。
- 完成后从哪项继续：从设置页健康检查开始，再做一次“输入 → 建议 → 预览差异 → 用户确认 → 写入 → 撤销”的真实模型 E2E。

## 2. 完成一次 Android 真机验收

- 问题：长期信任、自动发现、自动认证、自动重连、后台状态机和通知转消费路径已经通过自动化；仍缺真实手机权限与真实局域网环境证据。
- 用户需要做什么：在手机安装当前 NEXA Mobile；首次完成一次 QR/SAS 配对；授予 Android 通知使用权；按手机厂商设置允许必要的后台运行；重启手机和电脑后确认无需再次扫码；发送一条由用户控制的支付通知并在电脑端确认生成的消费草稿。
- 不做会影响什么：不影响 Desktop、合成 Fixture 或自动化测试，只影响 `REAL_ANDROID_ACCEPTANCE`、后台存活和真实通知采集验收。
- 完成后从哪项继续：从可信设备页观察 `PAIRED → OFFLINE/DISCOVERING → CONNECTED`，随后验证支付通知草稿、确认入账、重复通知去重和非支付通知排除。

## 3. 决定是否开启 Windows 登录后启动

- 问题：开机启动设置已提供且默认不静默启用。
- 用户需要做什么：在设置页按个人偏好开启或保持关闭“Windows 登录后启动星枢”。
- 不做会影响什么：关闭时需要手动启动 Desktop，手机会保持已配对但离线；不会丢失信任身份，也不会要求重新扫码。
- 完成后从哪项继续：若开启，下一次 Windows 登录后验证 Desktop/Gateway 启动并自动恢复已信任设备连接。

## 4. 选择并批准 APEX 探针 Target

- 问题：APEX 三档探针合同和既有 harness 复用已经完成，但项目中没有获批的国内/国外 HTTPS Target。
- 用户需要做什么：各选择一个自己控制或明确获批的境内/境外 HTTPS 小响应端点；如需完整测速，再提供允许固定上下行流量、有限额 payload 的专用端点，并确认隐私说明和 allowlist。
- 不做会影响什么：页面继续正确显示“探针待配置”，不会执行真实请求，也不会被标记为系统异常。
- 完成后从哪项继续：先跑 light，再跑 quality；full 仅在用户主动点击后执行。

## 5. 实体听音确认英美发音

- 问题：US/UK 本地 SAPI 选择、按钮、播放状态和自动化调用路径均已通过；最终 QA 进程未能完成可听声音的物理确认。
- 用户需要做什么：在学习中心分别点击“美音”和“英音”，确认两种声音可听、可区分，并检查 Windows 语音组件和输出设备。
- 不做会影响什么：不影响词卡、音标、义项、例句、词组和词形资料，只保留实体听音的人验缺口。
- 完成后从哪项继续：记录一次 US/UK 实体听音 PASS；若系统提示本地发音不可用，只修复 SAPI/音频设备可用性，不改为在线语音服务。

## 无需用户处理的 Creator 事项

8765 冲突已经由当前状态解除。不要 Kill 进程、不要改端口、不要手工运行 Python、不要创建第二套 Host。Creator Ops 应继续只由正式 Desktop 生命周期自然启动并在退出时停止。
