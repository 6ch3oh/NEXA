# NEXA Wave004 晨间人工待办

## 明早先启动这一版

启动：

`<PROJECT_ROOT>\01_source\token-monitor\dist-visual-daily-use-wave004\win-unpacked\Token Monitor.exe`

旧稳定版仍保留在：

`<PROJECT_ROOT>\01_source\token-monitor\dist-daily-use-rc-final-001\win-unpacked\Token Monitor.exe`

不要同时启动两个版本，以免本地端口和用户数据目录争用。

## 1. 自媒体运营：释放现有本地服务占用

- QA 时发现 `127.0.0.1:8765` 已由一个更早启动的本地 Python 服务占用，当前 Creator Ops 因 owner mismatch 进入“暂不可用”产品态。
- 请先通过原应用或服务的正常退出方式关闭该旧服务，再重新启动 Wave004；不要按本报告中的 QA 时 PID 直接强制结束进程，因为 PID 会变化。
- 不处理的影响：自媒体一级页面仍可进入、重试和返回，但首页摘要与模块真实内容保持“暂不可用”。
- 本轮没有终止该既有服务，也没有改写其数据。

## 2. 股票市场：完成用户侧网络授权或数据源配置

- 在“股票市场 → 打开模块设置”中完成该模块明确提示的网络或数据源配置；若需要账号、授权或密钥，请只在产品设置界面操作，不要把 Secret 粘贴到聊天、截图或报告中。
- 不处理的影响：股票市场仍显示产品化的“当前不可用”状态，但不会白屏或暴露原始错误。
- 本轮没有登录、授权、修改防火墙、写入 Credential 或发起真实交易。

## 3. 设备与网络：按需补齐真实观测

- 当前能读取部分本机/网络状态；无法确认的延迟、设备或连接字段保持“未知/暂不可用”，没有被伪造成 0 或正常。
- 如需完整设备列表，请在正常使用环境中完成设备配对或让设备产生一次真实同步，再手动刷新。
- 不处理的影响：模块与首页底栏仍可用，但部分字段继续保持未知。

## 4. 消费中心：真实空态不是故障

- 当前数据源没有真实消费记录，因此首页和详情页显示“暂无记录”。
- 如需金额、分类环和明细，请通过消费中心已有的正常导入/录入流程添加真实记录；不要为了填满首页录入示例生产数据。

## 安全提示

- 本轮未读取、写入或输出真实 Credential/Secret。
- 不要把整个 QA profile、Cookies、Credential Store 或 `.env` 加入版本控制。
- 本轮已删除自己创建的 4 个隔离 Electron 临时 profile；仅保留截图、空日志/视频目录和报告。
